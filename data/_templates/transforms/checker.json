{
  "check if this values i DOUBLE else apply on error": {
    "example": "2.2 > return 2.2",
    "rule": "checker",
    "type": "double",
    "onerror": {
      "customVal": "",
      "type": "customVal"
    }
  },
  "check if this values i BOOLEAN else apply on error": {
    "example": "2.2 > return false",
    "rule": "checker",
    "type": "bool",
    "onerror": {
      "customVal": false,
      "type": "customVal"
    }
  },
  "check if this values i DATE else apply on error": {
    "example": "2.2 > return false",
    "rule": "checker",
    "type": "date",
    "onerror": {
      "customVal": "",
      "type": "customVal"
    }
  },
  "check if this values i EMPTY else apply on error": {
    "example": "2.2 > return ''",
    "rule": "checker",
    "type": "empty",
    "onerror": {
      "customVal": "",
      "type": "customVal"
    }
  }
}
